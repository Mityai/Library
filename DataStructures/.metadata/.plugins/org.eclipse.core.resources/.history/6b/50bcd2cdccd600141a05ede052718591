/*
 * CartesianTreeImplictKey.cpp
 *
 *  Created on: Mar 30, 2015
 *      Author: dima
 */

#include <bits/stdc++.h>

struct Node {
	Node *l, *r;
	int key, prior;
	int w;

	Node() :
		l(nullptr), r(nullptr), key(0), prior(rand()), w(1) {};
};
