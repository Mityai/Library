/*
 * SparseTable.cpp
 *
 * minimum on range
 * [0..n)
 *
 *  Created on: Mar 29, 2015
 *      Author: dima
 */

#include <bits/stdc++.h>

const int N = int(1e5);
const int K = 20;

int a[N];
int f[K][N];
int logK[N];

int main() {
	int n;
	scanf("%d", &n);

	for (int i = 0; i < n; i++) {
		scanf("%d", &a[i]);
	}

	calculate(n);
}
